import { NEW_LOCATIONS_TYPE, SAVED_LOCATIONS_TYPE } from '../types/storage-keys.types';

export const SAVED_LOCATIONS: SAVED_LOCATIONS_TYPE = 'saved-locations';
export const NEW_LOCATIONS: NEW_LOCATIONS_TYPE = 'new-locations';

